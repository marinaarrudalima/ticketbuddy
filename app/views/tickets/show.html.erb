<p style="color: green"><%= notice %></p>
<div class="container">
<div class="card-trip">
  <img src="<%= image_path @ticket.artist.downcase.gsub(' ', '_') + '.jpg' %>" />
  <div class="card-trip-infos">
    <div>
      <h2><%= @ticket.artist %></h2>
      <p>The show will be held in <b><%= @ticket.city %></b></p>
    </div>
    <h2 class="card-trip-pricing">$<%= @ticket.price %></h2>
  </div>
       <%= link_to "Back to your search", :back %>
     <li class="list-group-item">
      <% if @ticket.user == current_user %>
        <p>You cannot buy your own ticket</p>
      <% elsif @ticket.pending? %>
        <%= link_to "Buy Ticket", ticket_orders_path(@ticket), data: {turbo_method: :post} %>
      <% else %>
       <p>This Ticket is not available anymore</p>
      <% end %>
    </li>
</div>
</div>
